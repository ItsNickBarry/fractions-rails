require 'test_helper'

class FractionCreateTest < ActionDispatch::IntegrationTest
  test "update fraction description" do
    skip 'update fraction description'
  end

  test "update fraction description repeatedly without changes" do
    skip 'update fraction description, expect no changes'
  end
end
