# == Schema Information
#
# Table name: electorate_memberships
#
#  id            :integer          not null, primary key
#  electorate_id :integer          not null
#  position_id   :integer          not null
#  caller        :boolean          default(FALSE), not null
#  electoral     :boolean          default(FALSE), not null
#  absolute      :boolean          default(FALSE), not null
#  weight        :float            default(1.0), not null
#  created_at    :datetime         not null
#  updated_at    :datetime         not null
#

danmark_folketing_danmark_folketing:
  electorate: danmark_folketing
  position: danmark_folketing

united_states_electoral_college_washington_representatives:
  electorate: united_states_electoral_college
  position: washington_representatives
